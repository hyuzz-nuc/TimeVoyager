# TimeVoyager — 环境配置记录

**版本：** 1.0  
**创建时间：** 2026-03-20  
**项目路径：** `E:\vibecoding\TimeVoyager`

---

## 一、开发环境

### 1.1 系统要求
| 系统 | 最低版本 | 推荐版本 |
|------|----------|----------|
| Windows | 10 | 11 |
| macOS | 10.15 | 12+ |
| Node.js | 18.x | 20.x+ |
| npm | 9.x | 10.x+ |

### 1.2 当前环境
```
操作系统：Windows 10.0.19045
Node.js: v24.13.0
npm: (内置)
```

---

## 二、项目初始化步骤

### 2.1 创建项目目录
```bash
mkdir E:\vibecoding\TimeVoyager
cd E:\vibecoding\TimeVoyager
```

### 2.2 初始化 Vite + Vue 项目
```bash
npm create vite@latest . -- --template vue
```

### 2.3 安装基础依赖
```bash
npm install
```

### 2.4 安装 UI 框架
```bash
npm install vuetify
```

### 2.5 安装 PWA 支持
```bash
npm install -D vite-plugin-pwa
```

### 2.6 安装 Electron
```bash
npm install -D electron electron-builder
```

### 2.7 安装路由
```bash
npm install vue-router@4
```

### 2.8 安装开发工具
```bash
npm install -D concurrently wait-on
```

---

## 三、package.json 配置

### 3.1 完整配置
```json
{
  "name": "time-voyager",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "TimeVoyager — 时光旅行者，专注即探索，时间换世界",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "electron:build": "vite build && electron-builder"
  },
  "dependencies": {
    "vue": "^3.5.30",
    "vuetify": "^4.0.3",
    "vue-router": "^4.x"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "concurrently": "^9.1.2",
    "electron": "^41.0.3",
    "electron-builder": "^26.8.1",
    "vite": "^8.0.1",
    "vite-plugin-pwa": "^1.2.0",
    "wait-on": "^8.0.3"
  }
}
```

### 3.2 脚本说明
| 脚本 | 功能 |
|------|------|
| `npm run dev` | 启动 Vite 开发服务器 |
| `npm run build` | 生产环境构建 |
| `npm run preview` | 预览生产构建 |
| `npm run electron:dev` | Electron 开发模式 |
| `npm run electron:build` | Electron 打包发布 |

---

## 四、Vite 配置

### 4.1 vite.config.js
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { VitePWA } from 'vite-plugin-pwa'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [
    vue(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.svg', 'icons.svg'],
      manifest: {
        name: 'TimeVoyager — 时光旅行者',
        short_name: 'TimeVoyager',
        description: '专注即探索，时间换世界，与时光伙伴同行',
        theme_color: '#6366f1',
        background_color: '#0f172a',
        display: 'standalone',
        icons: [
          {
            src: '/icons.svg',
            sizes: '192x192',
            type: 'image/svg+xml'
          },
          {
            src: '/icons.svg',
            sizes: '512x512',
            type: 'image/svg+xml'
          }
        ]
      }
    })
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  base: './',
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
})
```

### 4.2 关键配置说明
| 配置项 | 值 | 说明 |
|--------|-----|------|
| `base` | `'./'` | 相对路径，支持 Electron |
| `resolve.alias` | `@` → `./src` | 路径别名 |
| `build.outDir` | `'dist'` | 构建输出目录 |
| `PWA.registerType` | `'autoUpdate'` | 自动更新 Service Worker |

---

## 五、Vuetify 配置

### 5.1 src/plugins/vuetify.js
```javascript
import 'vuetify/styles'
import { createVuetify } from 'vuetify'
import * as components from 'vuetify/components'
import * as directives from 'vuetify/directives'

export default createVuetify({
  theme: {
    defaultTheme: 'timeVoyager',
    themes: {
      timeVoyager: {
        dark: true,
        colors: {
          primary: '#7c73e6',
          secondary: '#5c6bc0',
          accent: '#90caf9',
          background: '#0f172a',
          surface: '#1e293b',
          onBackground: '#e2e8f0',
          onSurface: '#f1f5f9',
          success: '#4ade80',
          warning: '#fbbf24',
          error: '#f87171'
        }
      }
    }
  },
  defaults: {
    VBtn: {
      style: 'text',
      rounded: 'xl'
    },
    VCard: {
      rounded: 'xl',
      elevation: 2
    }
  },
  components,
  directives
})
```

### 5.2 主题色说明
| 颜色名 | 色值 | 用途 |
|--------|------|------|
| primary | #7c73e6 | 主按钮、高亮 |
| secondary | #5c6bc0 | 辅助元素 |
| accent | #90caf9 | 强调、图标 |
| background | #0f172a | 页面背景 |
| surface | #1e293b | 卡片背景 |
| success | #4ade80 | 成功状态、步数 |
| warning | #fbbf24 | 警告、暂停 |
| error | #f87171 | 错误、停止 |

---

## 六、路由配置

### 6.1 src/router/index.js
```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/',
    name: 'Focus',
    component: () => import('@/views/FocusView.vue'),
    meta: { title: '专注', icon: 'mdi-timer-outline' }
  },
  {
    path: '/spirit',
    name: 'Spirit',
    component: () => import('@/views/SpiritView.vue'),
    meta: { title: '灵体', icon: 'mdi-star-four-points' }
  },
  {
    path: '/explore',
    name: 'Explore',
    component: () => import('@/views/ExploreView.vue'),
    meta: { title: '探索', icon: 'mdi-rocket-launch' }
  },
  {
    path: '/achievements',
    name: 'Achievements',
    component: () => import('@/views/AchievementsView.vue'),
    meta: { title: '成就', icon: 'mdi-trophy' }
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

router.beforeEach((to, from, next) => {
  if (to.meta.title) {
    document.title = `${to.meta.title} - TimeVoyager`
  }
  next()
})

export default router
```

---

## 七、入口文件

### 7.1 src/main.js
```javascript
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import vuetify from './plugins/vuetify'
import router from './router'

createApp(App)
  .use(vuetify)
  .use(router)
  .mount('#app')
```

### 7.2 插件注册顺序
1. Vuetify (UI 框架)
2. Router (路由)

---

## 八、Electron 配置（待完成）

### 8.1 electron/main.js（待创建）
```javascript
const { app, BrowserWindow } = require('electron')
const path = require('path')

function createWindow() {
  const win = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      nodeIntegration: true
    }
  })

  // 开发模式加载 Vite 服务器
  if (process.env.NODE_ENV === 'development') {
    win.loadURL('http://localhost:5173')
  } else {
    // 生产模式加载构建文件
    win.loadFile(path.join(__dirname, '../dist/index.html'))
  }
}

app.whenReady().then(createWindow)

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit()
  }
})
```

### 8.2 electron-builder 配置（待添加到 package.json）
```json
"build": {
  "appId": "com.timevoyager.app",
  "productName": "TimeVoyager",
  "directories": {
    "output": "release"
  },
  "files": [
    "dist/**/*",
    "electron/**/*"
  ],
  "win": {
    "target": "nsis"
  },
  "mac": {
    "target": "dmg"
  }
}
```

---

## 九、VSCode 插件推荐

| 插件名 | 用途 | 必要性 |
|--------|------|--------|
| Vue Language Features (Volar) | Vue 3 支持 | 必须 |
| ESLint | 代码检查 | 推荐 |
| Prettier | 代码格式化 | 推荐 |
| Auto Rename Tag | 标签重命名 | 推荐 |
| Material Icon Theme | 图标美化 | 可选 |

---

## 十、开发服务器

### 10.1 启动开发服务器
```bash
npm run dev
```

### 10.2 访问地址
- 本地：http://localhost:5173
- 局域网：http://<本机 IP>:5173

### 10.3 移动端测试
1. 手机和电脑连接同一 WiFi
2. 手机浏览器访问 `http://<电脑 IP>:5173`
3. 测试触摸交互和响应式布局

---

## 十一、构建与部署

### 11.1 生产构建
```bash
npm run build
```

### 11.2 预览构建
```bash
npm run preview
```

### 11.3 构建输出
```
dist/
├── index.html
├── assets/
│   ├── index-<hash>.js
│   ├── index-<hash>.css
│   └── ...
└── *.svg (PWA 资源)
```

### 11.4 GitHub Pages 部署（可选）
1. 构建项目：`npm run build`
2. 推送 `dist` 目录到 `gh-pages` 分支
3. 配置 GitHub Pages 源为 `gh-pages` 分支

---

## 十二、常见问题

### 12.1 依赖安装失败
```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules 和 lock 文件
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 12.2 Electron 下载超时
```bash
# 使用淘宝镜像
npm config set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
npm install
```

### 12.3 Vite 端口被占用
```bash
# 修改 vite.config.js
server: {
  port: 5174
}
```

---

**文档状态：** ✅ 已完成  
**最后更新：** 2026-03-20 18:30
